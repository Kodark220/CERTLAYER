{
  "name": "certlayer-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "api",
    "worker"
  ],
  "scripts": {
    "dev:frontend": "npm --workspace frontend run dev",
    "dev:api": "npm --workspace api run dev",
    "dev:worker": "npm --workspace worker run dev"
  }
}
